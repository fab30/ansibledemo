---
# Ansible demo
# On déploie l'application sur les cibles de type web
- hosts: web
  
  tasks:
    # On copie que le fichier sh dans /usr/bin, et on le rend executable
    - name: application.properties template
      copy: src=files/myapplication.sh dest=/user/bin/ owner={{ username }} mode=777